
let firstValue = Math.min() > Math.max();
let secondValue = Math.max()> Math.min();

console.log(Math.min(),Math.max());

// because Math.max return -infinity and Math.min() return infinity in Javascrit
// so that's why Math.min() > Math.max();


console.log(firstValue,secondValue)